CREATE TABLE users
(
	id serial PRIMARY KEY,
	fisrt_name varchar(30) NOT NULL,
	last_name varchar(30) NOT NULL
);

CREATE TABLE passports
(
	id serial PRIMARY KEY,
	passports_number integer NOT NULL,
	city_of_registration varchar(30) NOT NULL,
	fk_passports_users integer REFERENCES users(id)
);